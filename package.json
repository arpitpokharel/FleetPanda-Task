{
  "name": "fleet-panda-assesment",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation for SauceDemo test cases",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
